# Rust
target/
**/*.rs.bk

# Java
coord-spring-boot-starter/target/
java-example/target/
*.class
*.jar
*.war

# Node / coord-ui
coord-ui/node_modules/
coord-ui/dist/
# P0-G.2：pnpm-lock.yaml 纳入版本控制（镜像构建 --frozen-lockfile 可重现）

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Coord runtime data
data/
coord-dev-data/
*.db
*.snap

# Environment
.env
.env.local

# VS Code Copilot instructions (local only)
.github/copilot-instructions.md

# docs/ 默认不入库（内部计划/草稿）。但生产验证证据与**设计决策记录**必须入库
# （E4：「声称验证过但拿不出证据」在尽调中直接失分）——
# 放行 docs/production/evidence/ 与 docs/production/design/。
docs/*
!docs/production/
docs/production/*
!docs/production/evidence/
!docs/production/design/
