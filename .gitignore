# Rust
target/
**/*.rs.bk

# Java
# 注意：coord-spring-boot-starter 已于 2b55809 被整体删除（见 CONTRIBUTING.md），
# 此处不再为它保留 ignore 规则——保留会让“模块还存在”看起来像是真的。
java-example/target/
*.class
*.jar
*.war

# Node / coord-ui
coord-ui/node_modules/
coord-ui/dist/
# P0-G.2：pnpm-lock.yaml 纳入版本控制（镜像构建 --frozen-lockfile 可重现）

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Coord runtime data
data/
coord-dev-data/
*.db
*.snap

# Environment
.env
.env.local

# VS Code Copilot instructions (local only)
.github/copilot-instructions.md

docs/*
!docs/production/
docs/production/*
!docs/production/evidence/
!docs/production/design/
# 第四轮 §3.15：`docs/production/` 顶层的文档必须可入库——否则像
# `docs/volume-object-storage.md` 这种"被 tracked 文档引用的设计记录"只能存在于
# 本机磁盘上，对任何 clone 仓库的人都是死链。
!docs/production/*.md
# W6-1/W5-2/W9：运维面（runbook / SLO / 边界 / 治理 / 演练记录）必须可入库。
# 不写这一行的后果与第 41 行注释里描述的同型：`docs/production/*` 会把
# `docs/production/ops/` 整个目录排除掉，于是 P-Gate 7 要求的 runbook 只存在于
# 本机磁盘上——**看起来交付了，实际对任何 clone 都是 404**。
!docs/production/ops/

.coord-dev-cluster/
# 运行期产物（jepsen lab 的 nohup 日志等）
jepsen/lab/nohup.out

# perf 门禁的输出目录（`scripts/bench-ci.sh` 的 report / 临时 baseline）。
# CI 上它是临时目录；本地跑过之后会留下未跟踪文件。不写这一行的后果：
# `git status` 出现 `?? benchmark-results/`，而"证据 run 必须在 clean tree 上跑"
# 的判据恰恰看的就是这个字段 —— 一个可复现的假 DIRTY 来源。
/benchmark-results/

# CodeGraph 索引（本地工具产物，不入库）
.codegraph/
.ua/