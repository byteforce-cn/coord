[profile.dev]
# 1. 关闭调试符号（体积直接减半，编译速度还会变快）
# debug = 0

# 或者折中：只保留行号回溯，不要完整变量调试信息（体积减少约 30%）
# debug = 1

[source.crates-io]
replace-with = 'mirror'

[source.mirror]
registry = "sparse+https://mirrors.tuna.tsinghua.edu.cn/crates.io-index/"

[registries.mirror]
index = "sparse+https://mirrors.tuna.tsinghua.edu.cn/crates.io-index/"
